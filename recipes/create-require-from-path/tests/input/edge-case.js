/* we use createRequireFromPath beause ...*/ const { createRequireFromPath } /* we use createRequireFromPath beause ...*/ = require('module'); // we use createRequireFromPath beause ...

var r1 = createRequireFromPath('/path/to/module');
let r2 = createRequireFromPath('/path/to/module');
const r3 = createRequireFromPath('/path/to/module');
